kind: Service
apiVersion: v1
metadata:
  name: myapp-svc-headless
  namespace: stage
  labels:
    app: myapp
spec:
  selector:
    app: myapp
  type: ClusterIP
  clusterIP: None
  ports:
    - port: 4369
      targetPort: epmd
      name: epmd
      protocol: TCP
